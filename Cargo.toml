[package]
name = "async-drop"
version = "0.1.0"
edition = "2024"

[dependencies]

# Async 
futures = "0.3.32"
tokio = { version = "1.47.1", features = [
    "macros",
    "sync",
    "rt-multi-thread",
    "time",
] }
